Installation
============
First, ensure that you have the latest pip, which can be updated with the command::

   pip install --upgrade pip

Then, install *motulator* with pip::

   pip install motulator
