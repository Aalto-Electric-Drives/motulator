name: Build

on:
  workflow_dispatch: # Allows you to run this workflow manually from the Actions tab
  push:
    branches: [master]
  pull_request:
    branches: [master]


jobs:
  build-doc:
    runs-on: ubuntu-latest # Easy to use GitHub hosted runner, self hosted runner would require manual configurations
    steps:
    - uses: actions/checkout@v2 # Checks out your repository under $GITHUB_WORKSPACE, so your job can access it
    - name: Build Sphinx documentation
      uses: ammaraskar/sphinx-action@master # https://github.com/ammaraskar/sphinx-action
      with:
        pre-build-command: "python -m pip install sphinx m2r2 sphinx_rtd_theme && pip install numpydoc" # && python -m pip install -r requirements.txt & python -m pip install ."
        docs-folder: "docs/"
    # Publish built docs to gh-pages branch.
    # ===============================
    - name: Commit documentation changes
      run: |
        git clone https://github.com/JoonaKukkonen/motulator.git --branch gh-pages --single-branch gh-pages
        cp -r docs/_build/html/* gh-pages/
        cd gh-pages
        touch docs/.nojekyll
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git commit -m "Update documentation" -a || true
        # The above command will fail if no changes were present, so we ignore
        # that.
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        branch: gh-pages
        directory: gh-pages
        github_token: ${{ secrets.GITHUB_TOKEN }}
    # ===============================
