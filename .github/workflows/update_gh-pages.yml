name: update the gh-pages branch

on:
  workflow_dispatch: # Allows you to run this workflow manually from the Actions tab
  push:
    branches: [main]
  pull_request:
    branches: [main]


jobs:
  build-doc:
    runs-on: ubuntu-latest # Easy to use GitHub hosted runner, self hosted runner would require manual configurations
    steps:
    - uses: actions/checkout@v2 # Checks out your repository under $GITHUB_WORKSPACE, so your job can access it
    - name: Build Sphinx documentation
      uses: ammaraskar/sphinx-action@master
      with:
        pre-build-command: "python -m pip install sphinx m2r2 sphinx_rtd_theme\
        && pip install numpydoc && pip install sphinx-copybutton && pip install sphinx-autoapi\
        && pip install sphinx-gallery && pip install matplotlib && pip install motulator"
        # && python -m pip install -r requirements.txt && python -m pip install ."
        docs-folder: "docs/"
    # Publish built docs to gh-pages branch.
    # ===============================
    - name: Commit documentation changes
      run: |
        git clone https://github.com/JoonaKukkonen/motulator.git --branch gh-pages --single-branch gh-pages
        cp -r docs/build/html/* gh-pages/docs
        cd gh-pages
        touch docs/.nojekyll
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git commit -m "Update documentation" -a || true
        # The above command will fail if no changes were present, so we ignore
        # that.
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        branch: gh-pages
        directory: gh-pages
        github_token: ${{ secrets.GITHUB_TOKEN }}
    # ===============================
